# Kosongkan untuk memakai proxy Vite (/api -> VITE_API_PROXY_TARGET). Isi saat deploy (mis. https://api.indokerja.id).
VITE_API_URL=

# Target proxy Vite saat development, hanya dipakai jika VITE_API_URL kosong.
VITE_API_PROXY_TARGET=http://localhost:8080
